var email = document.getElementById("email");
email.innerHTML = "<%= j render "show", email: @email%>";

var liEl = document.getElementById("<%= @email.id %>");

<% if @email.read == false %>
  liEl.classList.add("unread")
<% else %>
  liEl.classList.remove("unread")
<% end %>